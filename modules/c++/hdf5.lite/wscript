NAME            = 'hdf5.lite'
VERSION         = '1.0'
MODULE_DEPS     = 'hdf5 str except io logging sys coda_oss config gsl'
USELIB_CHECK    = 'HDF5'

options = configure = distclean = lambda p: None

def build(bld):
    bld.module(**globals())
